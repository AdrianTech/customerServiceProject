// import axios from "axios";

const state = {
  services: []
};

const getters = {
  services: state => state.services
};

const mutations = {};
const actions = {};

export default {
  state,
  getters,
  mutations,
  actions
};
